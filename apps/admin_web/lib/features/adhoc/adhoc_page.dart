import 'package:flutter/material.dart';

class AdhocPage extends StatelessWidget {
  const AdhocPage({super.key, this.selectId});
  final String? selectId;
  @override
  Widget build(BuildContext context) => const Center(child: Text('AdhocPage – coming soon'));
}
