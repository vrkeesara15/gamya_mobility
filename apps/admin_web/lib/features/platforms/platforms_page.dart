import 'package:flutter/material.dart';

class PlatformsPage extends StatelessWidget {
  const PlatformsPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('PlatformsPage – coming soon'));
}
